/**:
  ros__parameters:
    yolo_model:
      config_file:
        name: carts-tiny.cfg
      weight_file:
        name: carts-tiny_best.weights
      threshold:
        value: 0.8
      detection_classes:
        names:
          - cart
